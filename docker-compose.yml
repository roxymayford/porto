services:
  porto-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: porto-website
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
